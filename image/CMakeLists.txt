cmake_minimum_required(VERSION 3.1.0 FATAL_ERROR)

# TODO: get rid of Qt

file(GLOB IMAGE_SOURCES *.cpp)
file(GLOB IMAGE_HEADERS *.h)
file(GLOB IMAGE_SHADERS *.vert *.frag)

set(SOURCES ${SOURCES} ${IMAGE_SOURCES} PARENT_SCOPE)
set(HEADERS ${HEADERS} ${IMAGE_HEADERS} PARENT_SCOPE)
set(SHADERS ${SHADERS} ${IMAGE_SHADERS} PARENT_SCOPE)